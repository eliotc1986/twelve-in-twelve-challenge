<h1>All users</h1>

 <% @users.each do |user| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag user.profile_image.url(:thumb), class: 'image'), user %>
        <em><%= user.email %></em>
      </div>
    </div>
  <% end %>

